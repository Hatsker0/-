<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Практическая работа №2 - </title> <!-- Замени на свою фамилию -->
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; line-height: 1.6; }
        h1, h2 { text-align: center; }
        p { font-size: 18px; margin: 20px 0; }
        form { margin: 50px 0; padding: 20px; background: #f0f0f0; border: 1px solid #ccc; }
        label { display: block; margin: 10px 0 5px; font-weight: bold; }
        select, input[type="number"] { padding: 8px; font-size: 16px; }
        input[type="checkbox"] { transform: scale(1.5); margin-right: 10px; }
        input[type="submit"] { : 10px 20px; font-size: 16px; background: #007bff; color: white; border: none; cursor: pointer; }
    </style>
</head>
<body>

    <h1>Практическая работа №2<br>Создание серверных сценариев PHP</h1>

    <?php
    // Задание 1
    // Объявляем две переменные с числами 8 и 9
    $num1 = 8;
    $num2 = 9;
    // Вычисляем сумму их квадратов (возведение в степень через **)
    $sum_squares = $num1 ** 2 + $num2 ** 2;
    // Выводим результат
    echo "<p><strong>Задание 1:</strong> Сумма квадратов $num1 и $num2 = $sum_squares</p>";


    // Задание 2
    // Объявляем три переменные
    $a = 3; $b = 5; $c = 8;
    // Вычисляем среднее арифметическое
    $avg = ($a + $b + $c) / 3;
    // Выводим результат
    echo "<p><strong>Задание 2:</strong> Среднее арифметическое чисел $a, $b, $c = $avg</p>";


    // Задание 3
    // Задаём значения x, y, z
    $x = 2; $y = 3; $z = 7;
    // Вычисляем выражение (x-1)*4 - 2*(z - 3*x² + y²)
    $res3 = ($x - 1) * 4 - 2 * ($z - 3 * $x ** 2 + $y ** 2);
    // Выводим результат
    echo "<p><strong>Задание 3:</strong> Результат выражения = $res3</p>";


    // Задание 4
    // Задаём три ненулевых числа
    $a4 = 2; $b4 = 5; $c4 = 7;
    // Вычисляем все варианты деления суммы двух чисел на третье
    $div1 = ($a4 + $b4) / $c4;
    $div2 = ($a4 + $c4) / $b4;
    $div3 = ($b4 + $c4) / $a4;
    // Выводим все три результата
    echo "<p><strong>Задание 4:</strong> 
          ($a4+$b4)/$c4 = $div1;<br>
          ($a4+$c4)/$b4 = $div2;<br>
          ($b4+$c4)/$a4 = $div3</p>";


    // Задание 5
    // Вычисляем 40% от 15 и 84% от 27
    $part1 = 15 * 0.40;
    $part2 = 27 * 0.84;
    // Суммируем их
    $sum5 = $part1 + $part2;
    // Выводим результат
    echo "<p><strong>Задание 5:</strong> Сумма 40% от 15 и 84% от 27 = $sum5</p>";


    // Задание 6
    // Задаём число
    $num6 = 16;
    // Проверяем условие: если больше 10 — прибавляем 100, иначе вычитаем 30
    if ($num6 > 10) {
        $num6 += 100;
    } else {
        $num6 -= 30;
    }
    // Выводим результат
    echo "<p><strong>Задание 6:</strong> Результат после преобразования = $num6</p>";


    // Задание 7
    // Задаём натуральное число
    $num7 = 6;
    // Проверяем чётность: если чётное — делим на 2, иначе умножаем на 3
    if ($num7 % 2 == 0) {
        $num7 /= 2;
    } else {
        $num7 *= 3;
    }
    // Выводим результат
    echo "<p><strong>Задание 7:</strong> Результат после преобразования = $num7</p>";


    // Задание 8
    // Задаём два числа
    $a8 = 7; $b8 = 3;
    // Тернарный оператор: выбираем большее
    $max = ($a8 > $b8) ? $a8 : $b8;
    // Выводим результат
    echo "<p><strong>Задание 8:</strong> Наибольшее число = $max</p>";


    // Задание 9
    // Задаём два числа
    $a9 = 21; $b9 = 152;
    // Вычисляем модуль разности
    $diff = abs($a9 - $b9);
    // Проверяем, равно ли разница 100
    $answer9 = ($diff == 100) ? 'Да' : 'Нет';
    // Выводим результат
    echo "<p><strong>Задание 9:</strong> Отличаются ли числа на 100? → $answer9</p>";


    // Задание 10
    // Задаём возраст (можно изменить для теста)
    $age = 85;
    // Тернарный оператор: если больше 80 — одно приветствие, иначе другое
    $greeting = ($age > 80) ? 'Здравствуйте, уважаемый' : 'Успехов!';
    // Выводим результат
    echo "<p><strong>Задание 10:</strong> Приветствие для возраста $age лет: $greeting</p>";


    // Задание 12
    // Создаём массив из трёх элементов
    $arr = ['Привет, ', 'мир', '!'];
    // Конкатенируем (склеиваем) элементы в одну строку и сохраняем в переменную $text
    $text = $arr[0] . $arr[1] . $arr[2];
    // Выводим содержимое переменной $text
    echo "<p><strong>Задание 12:</strong> $text</p>";
    ?>

    <!-- Задание 11: Форма для расчёта стоимости отдыха (в самом низу, чтобы не прыгала страница) -->
    <h2>Задание 11: Расчёт стоимости отдыха</h2>
    <form method="POST">
        <label>Выберите страну:</label>
        <select name="country">
            <option value="Россия">Россия</option>
            <option value="Грузия">Грузия</option>
            <option value="Испания">Испания</option>
            <option value="Абхазия">Абхазия</option>
        </select>

        <label>Количество дней отдыха:</label>
        <input type="number" name="days" min="1" required>

        <label>Есть ли у вас скидка 5%?</label>
        <input type="checkbox" name="discount">

        <br><br>
        <input type="submit" value="Рассчитать стоимость">
    </form>

    <?php
    // Обработка формы только после нажатия кнопки
    if ($_SERVER['REQUEST_METHOD'] === 'POST') {
        // Получаем данные из формы
        $country = $_POST['country'];                // выбранная страна
        $days = (int)$_POST['days'];                 // количество дней (приводим к целому числу)
        $discount = isset($_POST['discount']);       // true, если чекбокс отмечен

        // Базовая стоимость: 100 руб. за день
        $cost = $days * 100;

        // Надбавка за страну
        if ($country === 'Грузия') {
            $cost *= 1.10;    // +10%
        } elseif ($country === 'Испания') {
            $cost *= 1.12;    // +12%
        }
        // Россия и Абхазия — без надбавки

        // Применяем скидку 5%, если чекбокс отмечен
        if ($discount) {
            $cost *= 0.95;
        }

        // Выводим итоговую стоимость
        echo "<p><strong>Результат задания 11:</strong> Стоимость отдыха = $cost руб.</p>";
    }
    ?>

</body>
</html>
